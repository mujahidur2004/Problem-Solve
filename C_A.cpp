#include <bits/stdc++.h>
using namespace std;

#define fast_io ios::sync_with_stdio(false); cin.tie(nullptr);
#define ll long long

int main() {
    int l,s1,s2,q;
    cin>>l>>s1>>s2>>q;
    while(q--)
    {
        int a;
        cin>>a;
        
    }

    return 0;
}