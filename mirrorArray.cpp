#include<bits/stdc++.h>
using namespace std;
int main()
{
    int N,M;
    cin>>N>>M;
    vector<long long,<vector<long long>>A(N,vector<int>A(M));
    for(int i=0;i<N;i++)
    {
        for(int j=0;j<M;j++)
        {
            long long ele;
            cin>>ele;
            A.push_back(ele);
        }
    }
}